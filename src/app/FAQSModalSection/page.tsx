import React from 'react';
import Button from '@/components/ui/FAQSModalSection/buttons';

const page = () => {
    return (
        <div className='grid grid-cols-1 md:grid-cols-2 gap-4'>
            <Button text="SIS & AdDU Emails" />
            <Button text="paolo" />
            <Button text="SIS & AdDU Emails" />
            <Button text="paolo" />
            <Button text="SIS & AdDU Emails" />
            <Button text="paolo" />
        </div>

    );
};

export default page;
